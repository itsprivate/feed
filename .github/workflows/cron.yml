name: cron tasks
on:
  repository_dispatch:
    types: [schedule]
  workflow_dispatch:
    inputs:
      force_all:
        default: "0"
        description: "Force all sites to build"
        type: string
        required: false
      site:
        default: ""
        description: "Site to build"
        type: string
        required: false
      translate_count:
        description: "translate count"
        required: false
        default: "-1"
      translate_timeout:
        description: "translate timeout in minutes"
        required: false
        default: "120"
  push:
    branches:
      - main
    paths-ignore:
      - "**.md"
  # schedule:
  #   - cron: "*/20 * * * *"
jobs:
  build:
    runs-on: ubuntu-latest
    concurrency: build
    permissions:
      id-token: write
      contents: read
    steps:
      - name: Check out repository code
        uses: actions/checkout@v3
      - name: print aws cli version
        run: aws --version
      - id: install-aws-cli
        uses: unfor19/install-aws-cli-action@v1
        with:
          version: 2.22.35 # default
          verbose: false # default
          arch: amd64 # allowed values: amd64, arm64
      - name: print aws cli version
        run: aws --version
      - uses: actions/setup-node@v3
        with:
          node-version: 20
          cache: "npm"
      - name: install wrangler
        run: npm install -g wrangler
      - uses: denoland/setup-deno@v1
        with:
          deno-version: v2.5.4
      - uses: actions/cache@v3
        with:
          path: |
            ~/.deno
            ~/.cache/deno
          key: ${{ runner.os }}-deno-${{ hashFiles('**/*deps.ts') }}
      - run: make prod-load
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: ${{secrets.AWS_DEFAULT_REGION}}
          AWS_ENDPOINT: ${{secrets.AWS_ENDPOINT}}
      - name: Install Chrome for Puppeteer
        run: deno install --allow-scripts=npm:puppeteer --entrypoint puppeteer_deps.ts
      - name: Record baseline hash
        id: baseline
        run: |
          hash=$(sha256sum prod-current/items/bloombergnew/items.json 2>/dev/null | cut -d' ' -f1 || echo "none")
          echo "hash=$hash" >> $GITHUB_OUTPUT

      - run: make prod-source
        # - run: make prod-sourcesite site=china
        id: source
        continue-on-error: true
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          IM_DEEPL_AUTH_KEY: ${{secrets.IM_DEEPL_AUTH_KEY}}
          IM_DEEPL_TOKEN: ${{secrets.IM_DEEPL_TOKEN}}
          AWS_DEFAULT_REGION: ${{secrets.AWS_DEFAULT_REGION}}
          AWS_ENDPOINT: ${{secrets.AWS_ENDPOINT}}
          DEEPL_TRANSLATE_URL: ${{secrets.DEEPL_TRANSLATE_URL}}
          PRODUCTHUNT_TOKEN: ${{secrets.PRODUCTHUNT_TOKEN}}
          REDDIT_CLIENT_ID: ${{secrets.REDDIT_CLIENT_ID}}
          REDDIT_CLIENT_SECRET: ${{secrets.REDDIT_CLIENT_SECRET}}
          REDLIB_URL: ${{secrets.REDLIB_URL}}
          TRANSLATE_COUNT: ${{ github.event.inputs.translate_count }}
          TRANSLATE_TIMEOUT: ${{ github.event.inputs.translate_timeout }}
          ALL: ${{ github.event_name=='push' && '1' ||  github.event.inputs.force_all }}
          SITE: ${{ github.event.inputs.site }}
          GOOGLE_COOKIE: ${{secrets.GOOGLE_COOKIE}}
      - name: upload files
        run: make prod-upload
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: ${{secrets.AWS_DEFAULT_REGION}}
          AWS_ENDPOINT: ${{secrets.AWS_ENDPOINT}}
          DUFS_URL: ${{secrets.DUFS_URL}}
          DUFS_SECRETS: ${{secrets.DUFS_SECRETS}}
      - name: upload temp folder to github action for debug
        uses: actions/upload-artifact@v4
        with:
          name: temp
          path: temp
          if-no-files-found: ignore
      - name: throw if source failed
        if: steps.source.outcome == 'failure'
        run: |
          echo "::error::Source build failed"
          exit 1

      - name: Check for updates
        id: check_updates
        run: |
          hash=$(sha256sum prod-current/items/bloombergnew/items.json 2>/dev/null | cut -d' ' -f1 || echo "none")
          if [ "$hash" != "${{ steps.baseline.outputs.hash }}" ]; then
            echo "has_updates=true" >> $GITHUB_OUTPUT
          else
            echo "has_updates=false" >> $GITHUB_OUTPUT
          fi

      - name: Trigger deployment
        if: steps.check_updates.outputs.has_updates == 'true'
        run: |
          curl -X POST \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: token ${{ secrets.NEW_PERSONAL_TOKEN }}" \
            https://api.github.com/repos/tonormal/build/dispatches \
            -d '{"event_type":"deploy-external-app","client_payload":{"service":"bloombergnew","source_sha":"${{ github.sha }}"}}'
      - name: Publish pages
        if: true
        run: make prod-publishall
        timeout-minutes: 30
        env:
          SITE: ${{ github.event.inputs.site }}
          CLOUDFLARE_ACCOUNT_ID: ${{secrets.CLOUDFLARE_ACCOUNT_ID}}
          CLOUDFLARE_API_TOKEN: ${{secrets.CLOUDFLARE_API_TOKEN}}
